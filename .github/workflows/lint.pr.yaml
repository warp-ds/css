name: Lint

on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
    - uses: actions/checkout@v7
    - uses: pnpm/setup@v1
      with:
        runtime: node@lts
        cache: true
        install: true
    - run: pnpm lint
